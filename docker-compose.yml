version: "3.8"

services:
  nginx:
    build:
      context: ./
    container_name: nginx-con
    ports:
      - '80:80'
    restart: always
    networks:
      - web

networks:
  web:
    driver: bridge
